Discussion thread on a spot's detail view, each comment optionally carrying that person's vote tag.

```jsx
<CommentThread comments={[{author:'Josh Park', text:'We should go early before it gets packed.', time:'2d ago', vote:'must'}]} />
```
