Grid tile for a stored file in the Documents section.

```jsx
<DocumentTile title="Air Canada flight confirmation" type="Flight" dateLabel="Nov 8" />
```

Lay tiles out in a `grid-template-columns: repeat(auto-fill, 168px)` grid.
