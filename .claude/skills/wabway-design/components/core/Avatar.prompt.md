Member avatar — photo if available, else initials on a deterministic color from the trip's member palette.

```jsx
<Avatar name="Alex Kim" size="md" />
<Avatar name="Josh Park" src="/photo.jpg" ring />
```

See `MemberAvatarRow` (in the board/members group) for stacked overlapping groups of these.
