First-run / zero-result state for any list screen (Spots, Receipts, Documents…).

```jsx
<EmptyState icon={<Icon src="../../assets/icons/map-pin.svg" />} title="No spots yet" description="Add the first place worth visiting." action={<Button>Add a spot</Button>} />
```

Copy rule: name what's missing, then what to do — never a joke or a big illustration.
