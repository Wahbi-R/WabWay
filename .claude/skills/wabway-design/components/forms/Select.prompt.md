Native dropdown styled to match Input — category pickers, split method, document type.

```jsx
<Select label="Category" value={cat} onChange={e=>setCat(e.target.value)} options={[{value:'food',label:'Food'},{value:'temple',label:'Temple'}]} />
```
