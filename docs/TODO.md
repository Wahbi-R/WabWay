# Wabway – Feature TODO

Checked items are done. Log the date and a one-liner when completing something.

---

## Web + CI/CD (Do First)

- [x] **W1. Fix web build** — conditional export pattern for `share_handler` (native vs web stub) and `platform_file` helper replacing `dart:io` File usage; `flutter build web` compiles cleanly (2026-07-04, see `docs/features/web-build.md`).
- [x] **W2. GitHub Actions CI/CD** — on push to `main`: builds debug APK → creates/updates GitHub Release with APK attached; builds web → deploys to GitHub Pages; secrets-based `.env` creation (2026-07-04, see `docs/features/ci-cd.md`).
- [x] **W3. Custom domain** — CNAME configured in Porkbun DNS; custom domain set in GitHub Pages; SSL certificate approved and enforced by GitHub (2026-07-04, live at https://wabway.wabble.ca).
- [x] **W4. In-app update checker** — implemented as item #28; `UpdateChecker.check()` hits GitHub Releases API; `_UpdateBanner` shown in Home screen (Android only) (2026-07-04).

---

## High Priority

- [x] **1. In-app Import flow** — pick file/link → preview → choose type (Spot / Travel / Plan / Receipt / Document) → save via existing services (2026-07-03, see `docs/features/import-flow.md`)
- [x] **2. Place Search for Spots** — local Japan dataset, search fills name/address/coords/maps_url; "Map ready" indicator; "Open in Google Maps" action (2026-07-03, see `docs/features/place-search.md`)
- [x] **3. Google Maps link handling** — detect `google.com/maps` / `maps.app.goo.gl` / `goo.gl/maps`, extract `@lat,lng`, feed into Spot / Import / Share flows (2026-07-03, see `docs/features/maps-link-handling.md`)
- [x] **4. Map screen** — replace placeholder; show mapped spots with status colour; list/map toggle; tap opens spot detail (2026-07-04, see `docs/features/map-screen.md`)
- [x] **5. Android native share intake** — cold-start & while-open wired; Travel and Receipt destinations implemented; Maps URL title extraction improved; SEND_MULTIPLE intent added (2026-07-04, see `docs/features/share-intake.md`)
- [x] **6. Better document linking targets** — link docs to Receipt, Cash Withdrawal, Travel Item, Plan Item, Day, Spot, Trip from doc detail (2026-07-04, see `docs/features/doc-linking.md`)
- [x] **7. Attach ATM slips to cash withdrawals** — replaced placeholder with real photo-attach flow using `document_links` (2026-07-03)

---

## Medium Priority

- [x] **8. Links section** — replace placeholder screen; save title/URL/notes/category; category auto-detection; Realtime updates; open in browser (2026-07-04, see `docs/features/links.md`)
- [x] **9. Activity events** — real `activity_events` table/triggers; home screen reads from it with per-type icons and colours (2026-07-04, see `docs/features/activity-events.md`)
- [x] **10. Multi-trip switcher** — replace `trips.first`; trip switcher sheet; mobile access via More screen; desktop sidebar header tap (2026-07-04, see `docs/features/trip-switcher.md`)
- [x] **11. Improve invite system** — show all active/used/expired codes; revoke with confirm; single-use; expiry display; generate new code (2026-07-04, see `docs/features/invite-system.md`)
- [x] **12. Trip settings** — edit destination, dates, default currency via full settings sheet; owner-only; refreshes TripState (2026-07-04, see `docs/features/trip-settings.md`)
- [x] **13. Advanced Spots filtering** — filter icon wired; filter by status (multi-select) and city; badge shows active filter count (2026-07-04, see `docs/features/spots-filter.md`)
- [x] **14. Improve Plan editing** — edit day (city/date/notes); move item to another day; duplicate item; inline Move/Duplicate buttons in item detail (2026-07-04, see `docs/features/plan-editing.md`)
- [x] **15. Improve Travel editing** — booking status field (booked/tentative/cancelled) with badge on card and detail; doc-link sync on edit via syncDocLinks (2026-07-04, see `docs/features/travel-editing.md`)
- [x] **16. Receipt/document workflow improvements** — create receipt from doc detail; URL doc open in receipt linked-docs; split proportion bar + percentage labels (2026-07-04, see `docs/features/receipt-doc-workflow.md`)
- [x] **Make "Attach documents" collapsible in Add Plan Item sheet** — tap-to-expand with selected-count badge; auto-expands in edit mode (2026-07-04)

---

## Future / Considering

- [ ] **FUTURE-1. Live flight tracking + push alerts** — when a flight travel item has a flight number + date, a Supabase Edge Function (pg_cron, runs every ~20 min) polls a flight status API (AeroDataBox on RapidAPI recommended; ~$10/month basic tier, 500 req/month free) for flights departing within 48h; compares to last known state stored in a `flight_status` table; sends push notification to all trip members on delay, gate change, or cancellation. Architecture: `flight_number` + `date` already stored on travel_items; need `AERODATA_API_KEY` in server env; Edge Function queries travel_items for upcoming flights, hits `/flights/{flightNumber}/departures?dateLocal={date}`, diffs status, calls existing push notifier. Gate info reliable ~24h out; delays real-time. Ongoing API cost is the main consideration.

---

## Lower Priority / Later

- [x] **17. Settle Up flow** — `settlements` table; suggested settlements; mark as paid; persist; Realtime updates (2026-07-04, see `docs/features/settle-up.md`)
- [x] **18. Notifications** — settings UI with activity/money/docs/itinerary toggles; all dead bell/notification buttons routed to it; push delivery noted as future work (2026-07-04, see `docs/features/notifications.md`)
- [x] **19. Destination autocomplete** — local ~110-entry country/city dataset; `RawAutocomplete` in create and settings flows; no paid APIs (2026-07-04, see `docs/features/destination-autocomplete.md`)
- [x] **20. Owner transfer / leave-trip safety** — transfer ownership sheet (member picker); delete trip confirmation; leave trip hidden for owners (2026-07-04, see `docs/features/owner-transfer.md`)
- [x] **21. Branding polish** — adaptive launcher icons regenerated; splash screen uses brand cream (#F8F3EA) with centered icon (2026-07-04)
- [x] **22. Production cleanup** — `.env` gitignored confirmed; `.env.example` present; README updated with setup/build/migration instructions (2026-07-04)
- [x] **23. Offline / on-trip reliability** — stale data preserved on silent reload failure; `OfflineBanner` on Spots/Money/Travel screens when reload fails (2026-07-04)
- [x] **24. Global search** — search icon in Home AppBar; loads Spots/Docs/Travel/Receipts/Plan in parallel; grouped results by category (2026-07-04, see `lib/screens/global_search_screen.dart`)
- [x] **25. Empty / loading / error state polish** — links_screen no-flicker fix (silent reload); offline banners on Docs/Links screens; consistent retry actions (2026-07-04)
- [x] **26. Security / RLS review** — found and fixed 3 gaps: leaveTrip policy, ownership-transfer RPC (migration 003), settlements RLS; activity_events/trip_links RLS ensured (2026-07-04, see `supabase/migrations/003_rls_fixes_and_settlements.sql`)

---

## New / Upcoming

### High Priority

- [x] **27. App name branding fix** — renamed user-facing "Wabway" → "WabWay" in AndroidManifest label, main.dart title, sign-in screen, sidebar fallback, and member sheet error strings (2026-07-04).
- [x] **28. In-app update checker** — `UpdateChecker.check()` hits GitHub Releases API on app start (Android only, `!kIsWeb`); compares build number from `package_info_plus`; shows dismissible `_UpdateBanner` in Home screen with release notes and download link (opens APK via `url_launcher`); added `http: ^1.2.0` and `package_info_plus: ^8.0.0` to pubspec (2026-07-04).
- [x] **29. Profile editing** — edit name sheet already existed; fixed `ProfileState.updateShouldNotify` to also compare `displayName`/`avatarUrl` so name changes propagate to all screens immediately (2026-07-04).
- [x] **30. Remove member** — owner sees a remove icon on each non-owner member row; confirmation dialog; calls `TripService.removeMember`; refreshes TripState (2026-07-04).
- [x] **31. Document download + share** — Share button added to doc detail actions using `share_plus`; gets signed URL for file docs or uses `notes` URL for link docs; web guard shows snack instead (2026-07-04).
- [x] **32. Android App Links for magic link auth** — HTTPS intent filter added to `AndroidManifest.xml` for `wabway.wabble.ca` with `android:autoVerify="true"`; template `web/.well-known/assetlinks.json` created (deploy with GitHub Pages); **action needed**: replace `REPLACE_WITH_YOUR_SHA256_CERT_FINGERPRINT` in `assetlinks.json` with real fingerprint from `keytool -list -v -keystore release.jks` (2026-07-04).
- [x] **33. Mixed-currency settle-up** — added `calculateBalancesGrouped()` that groups balances per currency; `SettleUpPanel` refactored to accept `balancesByCurrency`/`suggestionsByCurrency` maps; renders separate balance cards per currency with currency badge when multi-currency; suggested payments list shows per-currency amounts (2026-07-04).
- [x] **34. Real place search (Photon API)** — replaced local dataset with live `photon.komoot.io` API; `searchPhoton()` returns structured name/city/area/address/coords; 350ms debounce + local fallback for immediate results while request is in flight; category inferred from OSM type; added `http: ^1.2.0` to pubspec (run `flutter pub get`) (2026-07-04).
- [x] **35. Edit spot** — edit sheet reuses add spot form pre-filled with existing values; edit pencil icon in detail title row and AppBar; `SpotService.updateSpot`; in-place update in spots_screen and map_screen lists (2026-07-04).
- [x] **36. "Confirmed" spot status + green map pin** — added `confirmed` to `SpotStatus` enum (between mustDo and planned); green badge + green map pin; migration `005_confirmed_spot_status.sql` runs `ALTER TYPE spot_status ADD VALUE IF NOT EXISTS 'confirmed'` (run in Supabase SQL editor); both `booked` and `confirmed` show as green (2026-07-04).
- [x] **37. Confirmed spots → Plan integration** — collapsible "Unplanned Spots" section above the days list in Plan screen; shows confirmed/planned spots not yet linked to any day; tapping opens `_DayPickerSheet` with day list + optional time picker; on confirm creates `ItineraryItem` with `linkedSpotId`; disappears from section once added (2026-07-04).

### Medium Priority

- [x] **37. Spot address field** — address text field added to add/edit spot form (pre-filled from Photon search); already shown in spot detail; edit form pre-fills from existing spot; `_addressCtrl` synced in both create and update paths (2026-07-04).
- [x] **38. Map tiles in English** — swapped to Carto Voyager tiles with `subdomains: ['a','b','c','d']`; English/romanized labels worldwide (2026-07-04).
- [x] **39. Map screen Realtime + fixes** — added Realtime subscription for `spots`/`spot_votes`; fixed `myVote: null` by computing from `spot.votes.*`; added refresh IconButton in AppBar (2026-07-04).
- [x] **40. Offline read cache** — `OfflineCache` service using `shared_preferences`; `SpotService`/`DocService` write raw rows on every successful load and expose `loadFromCache()`; Spots and Docs screens fall back to cache on cold-start failure and show offline banner (2026-07-04). Run `flutter pub get` for `shared_preferences: ^2.3.0`.
- [x] **41. Trip cover photo** — `_CoverPhotoField` in trip settings sheet; picks from gallery via `image_picker`; uploads to `trip-covers` Supabase storage bucket; shows preview with remove button; wired into `TripService.updateTrip()` (2026-07-04). Create `trip-covers` bucket in Supabase Storage (public) before using.
- [x] **42. Trip export** — "Export" button in Plan screen (mobile AppBar + desktop top bar); generates a plain-text itinerary with day/city/date headers, timed + untimed items, and notes; shares via `share_plus` system sheet; web shows a snack explaining it's not supported (2026-07-04).
- [x] **43. Calendar export** — `_exportToCalendar()` in plan_screen.dart generates a VCALENDAR/.ics with one VEVENT per itinerary item (date-only or datetime, with location/notes); written to `Directory.systemTemp` and shared via `share_plus` as `text/calendar`; calendar icon in mobile AppBar and desktop top bar; web guard shows a snack (2026-07-04).
- [x] **44. Push notifications (persistence)** — notification settings now persist across app restarts via `SharedPreferences` (`notif_activity/money/documents/itinerary` keys); loads on init with spinner guard; FCM/Edge Functions push delivery remains future work (2026-07-04).

### Lower Priority

- [x] **45. Plan item drag-to-reorder** — `_ReorderableItemList` with drag handles in `TripDayCard`; `sortOrder` added to `ItineraryItem` model and read by `_itemFromRow`; `sortedItems` respects `sortOrder` for untimed items; `PlanService.reorderItemsInDay()` persists new order; wired to both mobile and desktop plan layouts (2026-07-04).
- [x] **46. First-run onboarding** — 5-page `PageView` dialog shown on first launch (tracked in SharedPreferences `onboarding_v1_shown`); covers Welcome, Spots voting, Plan/itinerary, Money splits, and Documents; Skip and Next buttons; triggered from `home_screen.dart` via `showOnboardingIfNeeded()` (2026-07-04).
- [x] **47. Diagnostics screen** — shows app version, Supabase URL + connection check, user ID/name/email, active trip ID/name; copy-to-clipboard on each row; accessible via More → Debug (2026-07-04).
- [x] **48. QA checklist + test data reset guide** — `docs/QA_CHECKLIST.md` covers 13 test areas (auth, invite, spots/plan/money/docs two-device tests, onboarding, diagnostics, web build) and a SQL snippet for resetting test trip data (2026-07-04).

---

---

## Phase 3 — Maps Import & Media

### Completed

- [x] **49. Google Maps import** — share any Maps URL → WabWay; resolves short links via redirect tracking; Nominatim geocoding fallback; WebView scraper for JS-rendered list pages (5 regex patterns); KML import for My Maps (2026-07-07, see `docs/features/phase 3/google-maps-import.md`).
- [x] **50. Google Takeout CSV import** — fast sync parse + instant list display; three-step background geocoding per place (Nominatim name → URL slug → Google Maps page fetch); deduplication by URL; Notes column preserved; unfound places flagged "No location found" instead of dropped; progress counter "Finding locations… X of Y" (2026-07-07, see `docs/features/phase 3/google-takeout-import.md`).
- [x] **51. Spot images (Wikipedia thumbnails)** — `WikipediaImageService` fetches thumbnail URL from Wikipedia REST API (fallback: MediaWiki pageimages API); URL stored in `spots.image_url`; shown in list tile and detail header; no local storage (2026-07-07, see `docs/features/phase 3/spot-images.md`).
- [x] **52. OCR itinerary scanner** — two-pass OCR with `google_mlkit_text_recognition`; Gemini AI fallback for multi-column booking formats; PDF text-layer extraction before image OCR; parse mode shown in banner (2026-07-07, see `docs/features/phase 3/ocr-itinerary-scanner.md`).
- [x] **53. Changelog sheet** — auto-shows once on upgrade (compares `PackageInfo.buildNumber` to `SharedPrefs last_seen_build`); accessible from More → What's new; `DraggableScrollableSheet` with release list; const `_kChangelog` list in `changelog.dart` (2026-07-09, see `docs/features/phase 3/changelog-sheet.md`).
- [x] **54. Photo albums** — Photos screen under More; members add links to Google Photos / iCloud / Dropbox albums; service auto-detected from URL; step-by-step guide for Google Photos collaborative albums; Realtime sync; migration `006_trip_photo_albums.sql` applied via `supabase db push` (2026-07-09, see `docs/features/phase 3/photo-albums.md`).
- [x] **55. Audio transcription for share intake** — `wabway-server/` Python server (faster-whisper + spaCy NER, zero API cost) runs on LG G8 via Termux; Flutter gets "Extract from audio" banner in share screen (hidden when `AUDIO_SERVER_URL` unset); `SocialPlaceExtractor.extractFromAudio` POSTs to server, geocodes returned place names via Nominatim (2026-07-09, see `docs/features/phase 3/audio-transcription.md`).
- [x] **56. Unified import + share screen** — merged `_ImportSheet` (manual import) and `IncomingShareScreen` (Android share intent) into a single `IncomingShareScreen`; import mode shows source-picking step first (file picker, camera, URL paste) then synthesises an `IncomingShare` and continues identically; `showImportScreen()` replaces `showImportSheet()` in home and more screens; `itineraryNote` → `planItem` rename throughout `ShareDestination`; plan-item destination now fully wired with day picker and type chips (2026-07-09, see `docs/features/phase 3/unified-import-share.md`).

### Pending

- [ ] **56. Termux:Boot auto-start** — `pkg install termux-boot`; add `~/.termux/boot/start-server.sh` that runs `cd ~/wabway-server && bash start.sh`; keeps audio server running after phone reboot without manual intervention.
- [x] **57. Paste-caption fallback + server caption proxy** — `_PasteCaptionCard` in `IncomingShareScreen` lets users paste an Instagram/TikTok caption manually; `/caption` endpoint on `wabway-server` fetches captions server-side (yt-dlp primary, HTML scrape fallback) to bypass CORS; proxy URL registered via `OembedService.setProxyUrl`; auto-combined with audio transcript on `/transcribe` (2026-07-09, see `docs/features/phase 3/caption-extraction.md`).
- [x] **58. Photos create-album buttons** — empty state on Photos screen shows `_CreateAlbumCard` with Google Photos and iCloud launch buttons; note that photos don't auto-sync added to guide card subtitle (2026-07-09).
- [x] **59. Document image thumbnails** — `DocService.getThumbnailUrl` generates a 400×280 signed URL with Supabase Storage image transform (CDN-cached after first access); `DocGridCard` header and `DocListRow` leading box show `Image.network` with fade-in for image docs; non-image docs unchanged (2026-07-09, see `docs/features/phase 3/doc-thumbnails.md`).
- [x] **60. Multi-currency receipts** — `home_currency` on trips (default CAD, settable in Trip Settings); exchange rate auto-fetched from Frankfurter API at time of receipt creation and locked in; optional transaction fee field; `home_amount` stored once and never recalculated; ReceiptListTile shows original and home-currency amounts; migration `007_multi_currency_receipts.sql` applied via `supabase db push` (2026-07-09, see `docs/features/phase 3/multi-currency-receipts.md`).
- [x] **61. Auto-fetch spot thumbnails** — on spots load, spots with `imageUrl == null` are queued for Wikipedia thumbnail lookup in background; `_thumbnailAttempted` set prevents re-fetching; results streamed into list and written back to DB (2026-07-09).
- [x] **62. Plan item improvements** — spot picker at top of add-item sheet autofills title/address/city/country/mapsUrl and links the spot; location field now autocompletes via Photon; country field added to `ItineraryItem`; migration `008_itinerary_country.sql` (2026-07-09, see `docs/features/phase 3/plan-item-improvements.md`).
- [ ] **253. Trip connections cleanup** — migrate scattered `linked_spot_id`, `linked_doc_ids`, `linked_itinerary_item_id` columns into `trip_connections` and remove old columns + service methods (future sprint, after trip-connections feature is proven).
- [x] **255. Trip connections — plan item & doc detail** — ConnectionsSection wired into plan item detail and document detail screens; stays and links skipped (no dedicated detail screens) (2026-08-13, build 255).
- [x] **254. Trip connections — data layer + spot & travel detail** — new `trip_connections` table (migration 034) stores cross-entity links; `ConnectionsSection` widget + `ConnectionPickerSheet` added; wired into spot detail and travel item detail screens (2026-08-13, build 254). See docs/features/phase 3/trip-connections.md for remaining screens to wire up.
- [x] **252. Per-person packing checklist + plan icon centering** — packing list now uses per-member check-offs instead of single assignee; each item shows colored avatar dots for who has/hasn't packed it; progress bar shows your personal count; plan timeline item icons now vertically centered on multi-line titles (2026-08-13, build 252).
- [x] **251. Add to itinerary smart day suggestion** — "Add to itinerary" on a travel item now pre-selects and pre-fills the time for the plan day that matches the item's date; unselected suggested day shows a "Suggested" badge so it's easy to spot even if you pick a different day (2026-08-13, build 251).
- [x] **250. Home First Up tappable + spots color dots** — First Up items on the home screen are now tappable and navigate to the travel or plan item detail screen; spots list tiles show a small colored category dot (terracotta=food, sage=landmark/nature, gold=experience/shopping, purple=nightlife) (2026-08-13, build 250).
- [x] **249. UI polish: Settle Up centering, crew chat avatars, More screen fix** — Settle Up empty state now centered when all balances are zero; desktop Settle Up tab spans full width instead of left column only; crew chat messages show circular avatars beside each bubble; More screen settings icon missing from mockup fixed (2026-08-13, build 249).
- [x] **248. Travel: boarding pass scan + terminal/gate/seat fields** — "Scan boarding pass" button on flight form uses Gemini to extract terminal, gate, seat, boarding time, flight number, airports, and times and auto-fills the form; new departure terminal, arrival terminal, gate, seat, and boarding time fields added to all transit travel items; detail view shows them in the info card (2026-08-13, build 248).
- [x] **247. Travel types revamp + plan progress bar + test data** — Travel types changed from {flight, hotel, train, ticket, reservation, other} to {flight, train, bus, ferry, car, other} with updated icons/colors; plan progress bar now always visible when items exist (not just when some are done); inserted test Stays/Links/Docs into test trip (2026-08-13, build 247).
- [x] **246. UI parity: AppBar title weight, balance chip two-line layout, More screen explore grid** — AppBar title changed to `FontWeight.w800`; `_MemberBalanceChip` now stacks name above amount in a Column for clearer at-a-glance reading; More screen Explore section replaced vertical list with a 4×2 icon grid matching the mockup design (2026-08-13, build 246).
- [x] **245. Home AppBar: Lucide icons (MessageSquare, Search, Bell)** — replaced `Icons.chat_rounded`, `Icons.search_rounded`, and `Icons.notifications_outlined` with `LucideIcons.messageSquare`, `LucideIcons.search`, and `LucideIcons.bell` from `lucide_icons_flutter ^3.1.15`; also fixed stray apostrophe in changelog.dart that caused a parse error (2026-08-13, build 245).
- [x] **244. Home: balance chips, stat label fix, nextDay sort** — per-member balance chips replace the balance card (avatar initial + name + "Settled ✓" or amount); stat labels shortened to "Saved"/"Days"/"Spent" to prevent truncation; `nextDay` now explicitly sorts by date before picking so "First up" always shows the earliest upcoming day (2026-08-13, build 244).
- [x] **243. Fix: update banner in debug builds; Add day overflow with keyboard** — `UpdateCheckerBanner` skips the update check entirely when `kDebugMode` is true so the banner never shows in local debug runs; `_AddDayDialog` content column wrapped in `SingleChildScrollView` so the form scrolls when the keyboard is up instead of overflowing (2026-08-13, build 243).
- [x] **242. Home: pre-trip state improvements** — `_TripHero` adds a "First up" section (divider + up to 2 rows) showing the next travel booking and first plan item when the trip hasn't started; stat label changes from "Spots visited" to "Spots saved" pre-trip; standalone `_UpcomingCard` suppressed when pre-trip to avoid duplication (2026-08-13, build 242).
- [x] **197. Money: scan receipt & split items** — "Scan receipt & split items" button appears below the photo zone in the Add Receipt sheet once a photo is picked; calls `/receipt/scan` (AI OCR) and shows an item-level split sheet where you tap who had each item; tax/tip distributed proportionally; pre-fills amount and custom splits in the form; if scan fails (server down, timeout) falls back gracefully with a snackbar so manual entry still works (2026-08-13, build 197, see `docs/features/phase 3/receipt-scan-split.md`).
- [x] **196. Shopping: link & image on items** — `link_url` and `image_url` columns added to `shopping_items` (migration `032_shopping_item_url.sql`); Link field in item sheet auto-fetches the OG/Twitter image on blur; image preview shown in sheet with clear button; thumbnail (48×48) and link icon shown on tile; `ShoppingItem` model and `ShoppingService` updated (2026-08-13, build 196).
- [x] **195. Find Me auto-starts location sharing** — after sending a Find Me SOS, `LocationSharingManager.start()` is called automatically if not already sharing, so the sender's live map pin stays current while the crew navigates to them (2026-08-13, build 195).
- [x] **194. Web fixes & UX polish** — Fixed create trip stuck on loading spinner after creation (missing `Navigator.pop`); fixed Trip Settings and Invite sheets extending to infinity on web (replaced `DraggableScrollableSheet` with `ConstrainedBox`); added Delete/Leave trip to desktop Settings panel; replaced separate start/end date pickers with `showDateRangePicker` across trip creation, trip settings, member dates, and accommodation sheets; replaced create trip currency text field with labelled dropdowns (Spending currency + Settlement currency) with an "Other" escape hatch for custom 3-letter codes; currency list extracted to `lib/data/currencies.dart` (2026-08-12, build 194).
- [x] **153. Fix grey screen after login on web** — `NotificationService.instance` was accessed unconditionally in `AppShell.initState()`, triggering `FirebaseMessaging.instance` on web where Firebase is never initialized; this caused a Dart type cast failure that replaced the entire shell with Flutter's grey `ErrorWidget`; fix: guard the call with `if (!kIsWeb)` in `app_shell.dart` (2026-08-06, build 110).
- [x] **152. Crew location — Find Me, meetup point, Meet in the Middle** — 🆘 Find Me button in chat input bar sends a `find_me` message (red card with "Go to [name]" navigate button) and pushes an urgent notification to the crew; long-press on Live Map (or "Meet in the middle" pill button) sets a meetup point: confirms via dialog, inserts a `meetup_point` green card in chat with "Navigate to meetup" button, and pushes a crew notification; `_FindMeCard`, `_MeetupPointCard` widgets added; `MessageType.findMe` and `MessageType.meetupPoint` added to data model; `CrewService.sendFindMe/sendMeetupPoint` added; push notification also sent when location sharing starts (2026-08-06, build 109).
- [x] **151. Crew location — background sharing + navigate to member** — `getPositionStream` with `AndroidSettings(foregroundNotificationConfig)` replaces `Timer.periodic` so location sharing continues in the background with a persistent notification; `FOREGROUND_SERVICE` and `FOREGROUND_SERVICE_LOCATION` permissions + `GeolocatorForegroundService` service declaration added to AndroidManifest; tapping a crew member's marker on the Live Map opens a sheet with "Navigate to [name]" that launches the native maps app via `geo:` URI (Google Maps fallback); `AppleSettings` used on iOS (no background on iOS yet) (2026-08-06, build 108, see `docs/features/phase 3/crew-location-improvements.md`).
- [x] **150. Mobile web browser fixes** — cream loading screen with spinner replaces blank grey while Flutter initialises; viewport meta tag added for correct mobile layout; keyboard Search key race condition in place search fixed (`_suppressHide` flag); custom `flutter_bootstrap.js` paints cream background behind CanvasKit canvas and shows an error card if the engine fails to start (2026-08-06, build 107, see `docs/features/phase 3/mobile-web-fixes.md`).
- [x] **149. Google Places photos + home screen performance** — replaced Wikipedia thumbnail fetching with Google Places photo API via wabway-server `/places/photo` endpoint; `placeId` now carried through `PlaceSuggestion` → `add_spot_sheet` → save; CDN photo URL fetched at spot-create time and stored immediately; CORS-blocked Wikipedia calls removed from web; CI web build moved before APK and `--web-renderer html` (removed in Flutter 3.22) replaced with `--wasm`; `kStyle*` text styles changed from getters to `final` fields so `GoogleFonts.*()` is called once per app lifetime; `_ActivityFeed` precomputes O(1) lookup maps; heavy widgets wrapped in `RepaintBoundary` (2026-08-05, build 100, see `docs/features/phase 3/google-places-photos.md`).
- [x] **148. Planned cost on plan items** — `planned_cost NUMERIC` and `currency TEXT` columns added to `itinerary_items` (migration 019); cost field (amount + currency picker from a common list) added to the add/edit item sheet defaulting to trip's `defaultCurrency`; cost shown as a small chip on the item tile next to link badges; day card footer shows a running estimated total per currency when any item has a cost; `PlanService.createItem` and `updateItem` persist the new fields (2026-07-11, build 99).
- [x] **147. Clear packed items** — ⋮ menu appears in the packing screen AppBar when at least one item is packed; "Clear packed items" shows a confirmation dialog and bulk-deletes all packed items via `PackingService.clearPackedItems` (new method); optimistic local state update with silent rollback on error (2026-07-10, build 98).
- [x] **146. Delete plan day** — "⋮" popup menu replaces the pencil icon on every day card header; menu shows "Edit day" and "Delete day"; deleting shows a confirmation dialog listing the day number, city, and a warning that all itinerary items will be removed; confirmed delete removes the day optimistically from state and calls `PlanService.deleteDay`; wired in both desktop and mobile plan views (2026-07-10, build 97).
- [x] **145. Map category filter chips** — horizontal scrollable chip strip overlaid at top of map; shows each present category with count; tapping hides/shows that category's markers; hidden chips are dimmed; only appears when 2+ categories are present; filter applies to `_visibleSpots` used for map markers (2026-07-10, build 96).
- [x] **144. Packing list drag-to-reorder** — `SliverReorderableList` replaces the previous flat `ListView.builder`; drag handles (`≡`) shown on unpacked items when search is empty; `PackingService.reorderItems()` bulk-upserts `sort_order`; packed section uses `SliverList` and is not reorderable (2026-07-10, build 95).
- [x] **143. Spot status quick actions** — spot detail now shows both "Mark as visited" (green) and "Skip" (outlined) buttons for active spots; visited/skipped spots show "Undo → mark as confirmed"; `_markVisited`, `_markSkipped`, `_markConfirmed` all delegate to a shared `_setStatus(SpotStatus)` helper (2026-07-10, build 94).
- [x] **142. Receipt date range filter** — date range chip on the receipts list opens Flutter's `showDateRangePicker`; receipts are filtered to the selected range; chip shows "Jan 5 – Jan 12" when active with a clear X; works alongside category filter and search; empty state updated for the date range case (2026-07-10, build 93).
- [x] **141. Visited/skipped spot styling** — spots with `visited` status are dimmed to 65% opacity with a green checkmark overlay on the photo; `skipped` spots are dimmed to 45% with a grey X overlay; makes done spots visually distinct at a glance without removing them from the list (2026-07-10, build 92).
- [x] **140. Travel item countdown** — travel cards show a colored countdown label ("Today!", "Tomorrow", "In N days") below the date row for upcoming items; cancelled or past items are silent; label color matches the item type (2026-07-10, build 91).
- [x] **139. Mojibake fix** — bulk-replaced garbled UTF-8-as-Windows-1252 sequences (em/en dashes, ellipsis, middle dots, bullets, box-drawing chars) across `plan_screen.dart`, `doc_card.dart`, `day_card.dart`, `item_detail.dart`, and `travel_item_detail.dart` using explicit Unicode codepoint matching in PowerShell (2026-07-10, build 90).
- [x] **138. Packing list search** — `WabwaySearchBar` added to packing screen; filters by item title (unpacked and packed groups both filtered); shows a "No results" empty state when search yields nothing (2026-07-10, build 89).
- [x] **137. Packing item assignment** — packing tiles show "Assigned to [name]" (or "you") for unpacked items with an assignee; popup menu gains "Assign to..." / "Reassign" action opening a radio-button member picker; `PackingService.assignItem` writes `assigned_to` to Supabase with optimistic update (2026-07-10, build 88).
- [x] **136. Packing item rename** — packing tiles now show a three-dot popup menu with Rename and Delete actions; rename opens a pre-filled dialog with optimistic title update; `PackingService.renameItem` patches the title in Supabase; `PackingItem.copyWith` extended to support `title` (2026-07-10, build 87).
- [x] **135. Link editing** — saved links now have a kebab menu (open / edit / delete); edit opens the add-link sheet pre-filled with all existing fields; `LinksService.updateLink` patches title, URL, category, and notes via Supabase; optimistic update in `_LinksScreenState` (2026-07-10, build 86).
- [x] **134. Packing "packed by" credit** — packing tiles now show "Packed by Alex" (or "Packed by you") under the item title when packed, using the stored `packedBy` userId resolved to a display name via TripState members (2026-07-10, build 85).
- [x] **133. Stay check-in countdown** — accommodation cards now show a check-in countdown ("Check-in in 3 days", "Check-in tomorrow", "Check-in today!", or "Staying now") computed from today's date vs. the stored check-in/check-out dates (2026-07-10, build 84).
- [x] **132. Tap links in activity feed** — link-added activity events on the home screen are now tappable; the linked URL is loaded via `LinksService.loadLinks` in the home data parallel fetch and opened in the external browser on tap (2026-07-10, build 83).
- [x] **131. Packing list grouping** — packed items now appear under a "Packed (N)" section header at the bottom of the list; still-to-pack items remain at the top so you can focus on what's left (2026-07-10, build 82).
- [x] **130. Receipt search** — search bar added to the Receipts tab (mobile and desktop); searches title, category, and notes; composable with the existing category filter and sort order; empty state is context-aware (search vs category) (2026-07-10, build 81).
- [x] **129. `_fmtDate` → `isoDate` consolidation** — removed the redundant private `_fmtDate` wrapper from `money_service.dart`, `plan_service.dart`, and `travel_service.dart`; all date-to-ISO-string formatting now goes through the shared `isoDate()` in `date_utils.dart` (2026-07-10, build 80).
- [x] **128. Links in global search** — saved links now appear in global search results; searches title, domain, URL, and notes; tapping a link result opens the URL in the external browser; `LinksService.loadLinks` added to the parallel `Future.wait` fetch in `global_search_screen.dart` (2026-07-10, build 79, see `docs/features/phase 3/links-in-global-search.md`).
- [x] **127. Receipt duplication** — "Duplicate" option in the receipt detail overflow menu pre-fills the Add Receipt form with all existing values; creates a new receipt dated today; linked documents are not carried over; `isDuplicate` flag added to `showAddReceiptSheet` (2026-07-10, build 78, see `docs/features/phase 3/receipt-duplication.md`).
- [x] **126. Group chat link** — organisers can add a WhatsApp/Telegram/Signal/any URL to the trip in Trip Settings; a chat bubble icon appears in the Members screen AppBar for all members to open it with one tap; migration 018 adds `group_chat_url` to `trips` (2026-07-10, build 77, see `docs/features/phase 3/group-chat-link.md`).
- [x] **125. Day member presence indicator** — itinerary day cards show which members are present on each day, computed from arrival/departure dates (build 75); compact avatar row with present in accent, absent in grey; hidden when no member has dates or all are present (2026-07-10, build 76, see `docs/features/phase 3/day-member-presence.md`).
- [x] **124. Member trip dates** — members can set personal arrival and departure dates for a trip; dates shown on member cards with flight icons; current user can tap to open a date picker sheet; `TripService.updateMemberDates` handles null (clear) vs value; migration 017 adds `arrival_date` and `departure_date` to `trip_members` (2026-07-10, build 75, see `docs/features/phase 3/member-trip-dates.md`).
- [x] **123. Pinboard realtime** — home screen pinboard card and Pinboard screen now subscribe to Supabase Realtime; new pins from other members appear instantly with 400ms debounce; channel cleaned up on dispose (2026-07-10, build 74).
- [x] **122. Receipt CSV export** — share button in Money → Receipts exports current filtered/sorted receipts as a CSV with date, title, category, amounts, paid-by, and notes (2026-07-10, build 73).
- [x] **121. Trip pinboard** — shared notice board for group notes (check-in codes, meet times, reminders); pinned notes appear on home screen; post/unpin/delete from Settings → Pinboard; backed by `trip_pins` table (migration 016) (2026-07-10, build 72, see `docs/features/phase 3/trip-pinboard.md`).
- [x] **120. UI polish — receipt notes preview + spot comment count** — receipt notes now shown inline (1 line) in the receipt list tile; spot comment count shown next to vote summary in the spot list tile (2026-07-10, build 71).
- [x] **119. Plan item comments** — trip members can leave short group notes on any itinerary item; comments load on item open with avatar + timestamp; backed by `itinerary_item_comments` table (migration 015) (2026-07-10, build 70, see `docs/features/phase 3/plan-item-comments.md`).
- [x] **118. Spot visited status** — spots can now be marked "Visited" via a button inside the spot detail; new `SpotStatus.visited` enum value backed by migration 014; map markers and badges show green; "Mark as visited" button hidden when already visited or skipped (2026-07-10, build 69, see `docs/features/phase 3/spot-visited-status.md`).
- [x] **117. Global search includes packing list** — packing items now appear in global search results under "Packing list" group; packed items show "Packed ✓" subtitle (2026-07-10, build 68).
- [x] **116. Trip hero cover photo** — home screen trip card now shows a 120px cover photo banner when a cover image is set; gradient overlay + edit icon repositioned into image; fade-in on load (2026-07-10, build 67, see `docs/features/phase 3/trip-hero-cover-photo.md`).
- [x] **115. Trip budget tracking** — optional budget field in Trip Settings; home screen shows a live "spent / budget" progress bar below the balance card; bar turns red when over budget; backed by `budget` column on `trips` table (migration 013) (2026-07-10, build 66, see `docs/features/phase 3/budget-tracking.md`).
- [x] **114. Today's agenda card — inline done toggle** — tap any item in the Today home card to mark it done/undone without navigating to the Plan screen; optimistic update with rollback on error; done counter updates immediately (2026-07-10, build 65, see `docs/features/phase 3/today-card-toggle.md`).
- [x] **113. Packing list** — shared checklist accessible from More → Packing List (mobile) and desktop sidebar; tap to toggle packed/unpacked with animated checkbox; realtime sync; per-member RLS via `packing_items` table (migration 012) (2026-07-10, build 64, see `docs/features/phase 3/packing-list.md`).
- [x] **112. Emergency info card** — Settings screen now has an Emergency Info tile; stores insurance, card emergency line, local emergency number, nearest hospital, embassy contacts, and notes; phone numbers open the dialer, policy numbers and addresses are copyable; backed by `trip_emergency_info` Supabase table (migration 011) (2026-07-10, build 63, see `docs/features/phase 3/emergency-info.md`).
- [x] **111. Currency converter** — offline bottom sheet on Money screen; derives rates from stored receipt exchange_rate values; shows home-currency equivalent and the rate used; works without internet (2026-07-10, build 62, see `docs/features/phase 3/currency-converter.md`).
- [x] **110. Quick expense FAB** — floating action button on the home screen opens the Add Receipt sheet directly; cuts tap count from 4 to 1 for logging expenses on the go; home screen refreshes after sheet closes (2026-07-10, build 61, see `docs/features/phase 3/quick-expense-fab.md`).
- [x] **109. Copy to clipboard — spot address + travel confirmation number** — tapping the address row in spot detail or the Confirmation/Address rows in travel item detail copies the value and shows a "Copied" snackbar; `_MetaRow` accepts `copyable: bool`; copy icon shown inline (2026-07-10, build 60, see `docs/features/phase 3/copy-to-clipboard.md`).
- [x] **108. Today's agenda card** — home screen shows a full "Today" card with all plan items when today is a plan day; displays done/total counter, type icons, times, and strikethrough for done items; replaces the single-item "Coming up" card while the trip is active (2026-07-10, build 59, see `docs/features/phase 3/today-agenda-card.md`).
- [x] **107. isoDate() consolidation** — added `isoDate(DateTime)` to `lib/data/date_utils.dart`; replaced 5 private ISO-date formatting blocks in `plan_service`, `travel_service`, `money_service`, `trip_service` (x2) (2026-07-10, build 58).
- [x] **106. Spending by member card** — "Spending by member" summary card added to the Receipts tab; shows each member's total paid with proportional bar; auto-hides when only one person paid; appears alongside the existing category breakdown (2026-07-10, build 57, see `docs/features/phase 3/spending-by-member.md`).
- [x] **105. WabwayFilterStrip generic widget** — extracted `WabwayFilterStrip<T>` to replace 3 identical private filter strip classes in Accommodations, Money, and Links screens; generic over the filter enum type; supports auto-hide, "All" chip, and per-value counts (2026-07-10, build 56, see `docs/features/phase 3/filter-strip-widget.md`).
- [x] **104. Accommodations offline resilience** — added `_offline` flag + `OfflineBanner` to `AccommodationsScreen`; silent realtime reload failures now show the banner instead of dropping silently; clears on successful reload (2026-07-10, build 55, see `docs/features/phase 3/accommodations-offline.md`).
- [x] **103. Settings screen trip section** — replaced "Coming soon" placeholder in Settings with working tiles for Trip Settings and Invite Members; removed dead `PlaceholderScreen` class that was never instantiated (2026-07-10, build 55, see `docs/features/phase 3/settings-screen-fix.md`).
- [x] **102. Date formatter consolidation** — created `lib/data/date_utils.dart` with single `fmtDate`; removed duplicate `fmtDate` (money_data.dart) and `fmtDayDate` (plan_data.dart); eliminated private `_fmtDate` copies in doc_card.dart and accommodations_screen.dart; both data files re-export `fmtDate` so callers need no import changes (2026-07-10, build 55, see `docs/features/phase 3/date-utils-consolidation.md`).
- [x] **101. Loading state refactor (WabwayLoadingScaffold + WabwayLoadingIndicator)** — extracted two shared loading widgets; `WabwayLoadingScaffold` replaces the early-return pattern in 5 screens (money, spots, accommodations, docs, travel); `WabwayLoadingIndicator` handles the inline body pattern in 3 screens (links, photos, plan); eliminated 8 copy-pasted 12-line Scaffold blocks (2026-07-10, build 54, see `docs/features/phase 3/loading-widgets.md`).
- [x] **100. Accommodations in global search** — stays now searchable from the global search screen; searches name, city, address, and notes; tapping a result opens the edit sheet for that accommodation; parallel load alongside the existing 5 sources (2026-07-10, build 53).
- [x] **99. Day completion badge** — day card header now shows "X/Y" done/total when items are checked off; badge turns green when every item on the day is done; shows plain count when nothing is done yet (2026-07-10, build 52).
- [x] **98. Mark plan item as done** — tapping the timeline dot on any itinerary item toggles it done/undone; done items show a green checkmark dot, strikethrough title, dimmed text, and hidden chevron; state persists via `is_done` boolean column (migration `010_itinerary_item_done.sql`); optimistic UI update with background sync via `PlanService.toggleDone`; both desktop and mobile plan views wired (2026-07-10, build 51).
- [x] **65. Google Places search + shared PlaceSearchField** — submit-triggered place search (Enter or icon tap) via wabway-server `/places/search` (Google Places API) with Photon fallback; shared `PlaceSearchField` widget replaces inline search in `add_spot_sheet.dart` and `_LocationField` in `add_item_sheet.dart`; `TakeoutParser.geocodePlace` uses Google Places as step 0 before Nominatim; server `/places/search` endpoint added to `main.py` (2026-07-10, see `docs/features/phase 3/google-places-search.md`).
- [x] **66. App architecture doc** — `docs/architecture.md` covers all 13 feature areas with data flows, connections between features, nav model, state model, env vars, and known redundancies/duplications (2026-07-10).
- [x] **67. Spot sorting** — sort toggle (Newest / A–Z / Most voted) in Spots screen AppBar (mobile & desktop); sort applied after filters and search; `_SpotSort` enum + `_filtered` getter handles all three modes; "Most voted" ranks by must-do count then total votes (2026-07-10, build 31).
- [x] **68. Trip countdown chip** — trip hero on Home shows a context-aware chip: "Starts in N days", "Day X of Y", or "Ended N days ago"; uses trip `startDate`/`endDate`; hidden when no dates are set (2026-07-10, build 31).
- [x] **69. Code deduplication** — fixed `_loadAll`/`_silentReload` duplication in `plan_screen.dart` (merged into single method with `silent` param); extracted `_pushWithState` helper in `more_screen.dart` replacing 6× repeated nav boilerplate; deduplicated AppBar `actions` list in `home_screen.dart` via `_appBarActions` method; removed dead `_showEditTripNameSheet` + unreachable `_EditTripNameSheet` from `more_screen.dart` (2026-07-10, build 31).
- [x] **70. Spot CSV export** — "Export spots as CSV" button in Spots screen AppBar and desktop top bar; exports currently visible spots (after filter + sort) as a standard CSV with name/city/area/country/category/status/address/mapsUrl/notes columns; uses `share_plus` so user can save or forward from the native share sheet (2026-07-10, build 32).
- [x] **71. Tappable trip hero** — trip owner can tap the home screen hero card to open Trip Settings inline; small edit pencil icon shown as visual hint; non-owners are unaffected; `showTripSettingsSheet` already available on the More screen (2026-07-10, build 32).
- [x] **75. Multi-currency home balance card** — balance card on home screen now shows debts/credits per currency separately; total spent uses home-currency equivalents (`homeAmount`) for a consistent cross-currency total; removes the single-currency limitation that previously showed only the biggest debt in one currency (2026-07-10, build 34).
- [x] **76. Accommodations realtime** — added Supabase Postgres CDC subscription to `AccommodationsScreen`; changes made by other trip members now appear automatically without a manual refresh (2026-07-10, build 34).
- [x] **73. Global search — tappable results** — search results now navigate directly to the relevant detail screen on tap; spots open `SpotDetailScreen`, docs open `DocDetailScreen`, travel opens `TravelItemDetailScreen`, receipts open `ReceiptDetailScreen`, plan items open `ItemDetailScreen`; a chevron icon is shown as a hint; `showGlobalSearch` now accepts `tripName`, `userId`, and `members` so detail screens have the context they need (2026-07-10, build 33).
- [x] **74. Travel screen dedup** — merged `_loadAll` and `_silentReload` in `travel_screen.dart` into a single method with `silent` flag, same pattern as `plan_screen.dart` (2026-07-10, build 33).
- [x] **72. Architecture doc + comments** — `docs/architecture.md` created with full feature map, data flows, redundancy table, env vars; `trip_state.dart` annotated with InheritedWidget usage guide; `home_screen.dart`, `plan_screen.dart`, `more_screen.dart`, `spots_screen.dart` all have human-style comments on non-obvious logic (2026-07-10).
- [x] **77. Spot notes preview in list tile** — spot notes now shown inline in the `SpotListTile` (up to 2 lines, ellipsised); notes appear between the city/category row and the vote summary so the most useful info is visible without tapping; hidden when notes are empty (2026-07-10, build 35).
- [x] **78. Receipt category filter** — horizontally scrolling category chips on the Receipts tab (mobile and desktop); only categories that appear in the current list are shown; count shown on each chip; auto-hidden when all receipts share the same category (2026-07-10, build 36).
- [x] **79. Travel items sorted by departure date** — travel items now sort chronologically by departure date so the list reads like a timeline; items with no date (draft bookings) fall to the end (2026-07-10, build 36).
- [x] **80. Tappable activity feed** — activity events on the home screen now navigate directly to the relevant detail screen on tap (spots, documents, travel items, receipts, plan items); a chevron hint is shown on tappable events; member-joined, link-added, and cash-withdrawal events are non-tappable as they have no detail screen (2026-07-10, build 37).
- [x] **81. Remove dead "Settle up" button from home screen** — the balance card's "Settle up" button was a no-op placeholder; removed it and the stale TODO comment; settle-up is fully available in the Money tab (2026-07-10, build 37).
- [x] **82. Cash withdrawal notes preview** — withdrawal notes now shown in the list tile (1 line) so you can see ATM location reminders without opening the detail; consistent with the spot notes preview added in build 35 (2026-07-10, build 38).
- [x] **83. Code comments pass** — added human-style comments in `spots_screen.dart` explaining `_thumbnailAttempted` guard and the fire-and-forget thumbnail fetch pattern (2026-07-10, build 38).
- [x] **84. Links category filter** — horizontally scrolling category chips in the Links screen; only categories present in the loaded links are shown with counts; auto-hides when all links share the same category; uses `CustomScrollView` + `SliverList` so the strip scrolls with the content (2026-07-10, build 39).
- [x] **97. WabwaySearchBar shared widget** — extracted `WabwaySearchBar` to `lib/widgets/wabway_search_bar.dart`; replaced repeated `TextField` search implementations in Links, Travel, Plan, and Stays screens; exported from `widgets.dart` (2026-07-10, build 50).
- [x] **96. Feature catalogue** — created `docs/feature-catalogue.md` documenting all 18 feature areas (Auth, Trips, Home, Spots, Links, Map, Plan, Travel, Stays, Money, Docs, Members, Photos, Global Search, Import, Share Intake, More, Realtime) including how each works, what it connects to, and a redundancy/consolidation section (2026-07-10).
- [x] **95. Travel type filter counts** — travel type chips (Flight, Train, Hotel, etc.) now show counts and are hidden when fewer than 2 types are present; consistent with spots/links/accommodations filter pattern (2026-07-10, build 49).
- [x] **94. Spot category filter counts** — category chips on the Spots screen now show counts ("Food (5)", "Activity (3)"); categories with no spots are hidden to keep the strip compact; consistent with the Links screen behaviour (2026-07-10, build 48).
- [x] **93. Stays search** — search bar added to Accommodations screen above the status filter; searches name, city, address, and notes; composable with the status filter chips; empty state is context-aware (no items vs. no results) (2026-07-10, build 47).
- [x] **92. Plan search** — search bar added to Plan screen (mobile and desktop); searches item title, location, city, and notes across all days; results shown as a flat list with day context ("Day 3 · Tokyo"); works alongside the existing day-card timeline (2026-07-10, build 46).
- [x] **91. Receipt date grouping** — receipts are grouped under date headers ("Mon, Jul 7") when sorted by date (newest/oldest); headers are omitted for amount-based sorts; implemented as a `_ReceiptListEntry` sealed class with `_DateHeader` and `_ReceiptItem` subtypes (2026-07-10, build 45).
- [x] **90. Code comments pass (build 44)** — added human-style comments to `money_screen.dart` (pending sync badge, derived-state pipeline), `global_search_screen.dart` (parallel Future.wait strategy, closure capture guard), and `links_screen.dart`; fixed missing semicolon parse error in `incoming_share_screen.dart` (2026-07-10, build 44).
- [x] **89. Travel search** — search field at top of Travel screen (mobile and desktop); searches title, location, destination, confirmation number, and notes; works together with the existing type-filter chips; empty state is context-aware (no results vs. no items) (2026-07-10, build 43).
- [x] **88. Receipt sorting** — sort receipts by newest/oldest date or highest/lowest amount via a sort menu next to the filter chips (mobile AppBar and desktop list header); sort applies after the category filter; icon turns blue when a non-default sort is active; uses `homeAmount` for amount sorts so cross-currency comparisons are consistent (2026-07-10, build 42).
- [x] **87. Links search** — search bar added to Links screen; searches title, domain, and notes; works in combination with the category filter so results can be narrowed by both type and keyword; clear button dismisses the query; empty-state message is context-aware (search vs category) (2026-07-10, build 41).
- [x] **86. Spending breakdown by category** — a card at the top of the Receipts tab showing total spent (home currency equivalent) and a per-category row with icon, amount, and a mini proportional bar; auto-hidden when all receipts are in the same category; uses `homeAmount` so mixed-currency trips show a consistent total (2026-07-10, build 40).
- [x] **85. Map screen silent realtime reload** — realtime spot changes no longer trigger a full loading spinner; debounced 400ms silent reload pattern (same as every other screen) keeps the map visible while updates stream in (2026-07-10, build 39).
- [x] **64. CSV file sharing + file type registry** — WabWay appears in Android share menu for CSV/XLS/XLSX; sharing a CSV goes straight to Takeout import; `FileTypeRegistry` centralises all accepted extensions and `ShareContentType` mapping — single edit point for new types; AndroidManifest has comment to keep MIME list in sync (2026-07-09, see `docs/features/phase 3/csv-file-support.md`).
- [x] **63. Spot country + better Maps import** — `country` field added to `Spot` model and DB (migration 009); Maps URL paste in add-spot form now resolves short links, extracts place name, and reverse-geocodes city/country via Nominatim; share flow pre-fills name/city/country when a Maps link is shared (2026-07-09, see `docs/features/phase 3/spot-country-maps-import.md`).
- [x] **150. Add to Plan from Spot Detail** — "Add to Plan" button added to every spot detail screen; taps lazily load trip days, opens the shared day picker sheet (day + optional time), then creates a linked itinerary item without a separate form; `showDayPickerSheet` extracted from `plan_screen.dart` to `lib/screens/plan/day_picker_sheet.dart` so both Plan and Spots can share it; `SpotCategory` mapped to `ItineraryItemType` (food→food, nightlife→activity, rest→spot) (2026-08-06, build 102, see `docs/features/phase 3/build-102-improvements.md`).
- [x] **151. Crew Chat on mobile Home + More** — chat icon added to Home screen AppBar for one-tap access to `CrewScreen` without opening More; in More screen Crew Chat promoted to a standalone prominent card (same style as Trip Switcher) above the Members section; removed from the generic Explore list to avoid duplication (2026-08-06, build 102, see `docs/features/phase 3/build-102-improvements.md`).
- [x] **152. Hotel → Stays prompt in Travel** — after saving a hotel travel item a 6-second snackbar offers "Save to Stays too?"; tapping Save calls `AccommodationService.create` with the hotel name, check-in, and check-out dates pre-filled and status `booked`; single tap, no sheet required (2026-08-06, build 102, see `docs/features/phase 3/build-102-improvements.md`).
- [x] **153. Plan screen offline banner** — when a silent realtime reload fails `plan_screen.dart` now shows the shared `OfflineBanner` with Retry; consistent with Money, Travel, and other screens (2026-08-06, build 102, see `docs/features/phase 3/build-102-improvements.md`).
- [x] **154. Android package rename** — changed package ID from `com.example.wabway` to `ca.wabble.wabway` (reverse domain for wabway.wabble.ca); updated `build.gradle.kts`, `AndroidManifest.xml`, `MainActivity.kt` package directory, `assetlinks.json` fingerprint, and all Dart files referencing the old package name (2026-08-06, build 103, see `docs/features/phase 3/push-notifications.md`).
- [x] **155. FCM push notifications** — full end-to-end push notification support for Android via Firebase Cloud Messaging; `device_tokens` Supabase table with RLS; `send-notification` Edge Function with service-account JWT auth; `NotificationService` singleton (token save/refresh, tap-to-deep-link via `TabSwitcher`); `pushNotify()` helper with per-category pref check; crew message, spot added, receipt added, and travel item events wired; Notification Settings screen now shows real toggles instead of "coming soon" (2026-08-06, build 103, see `docs/features/phase 3/push-notifications.md`).
- [x] **156. Update banner install hint** — "Download update →" now links to the GitHub releases page (not direct APK binary) so users see the full release; added a hint below the link explaining to open the download from the notification bar to install (2026-08-06, build 103).
- [x] **157. Import + Spot search fixes (6 bugs)** — (1) Japanese/CJK mojibake in Takeout CSV fixed by re-encoding W1252-interpreted chars back to their original UTF-8 bytes; (2) import cards show "Searching…" spinner during active geocoding instead of premature "No location found"; (3) save shows "Adding X of Y…" progress, processes spots in parallel batches of 5, and auto-closes on completion; (4) spots with no city name now get a Nominatim reverse-geocode after the Google Maps URL coord-fallback so "Unknown" is no longer stored; (5) spot list tile suppresses "Unknown" city instead of displaying it; (6) keyboard search button in Add Spot now works — `_onFocusChange` no longer hides results while a search is in-flight; (7) search field hint updated and "Add city or country for better results" tip shown below the search field (2026-08-06, see `docs/features/phase 3/import-spot-search-fixes.md`).
- [ ] **239. Offline Phase 3 — write queue** — queue mutations (add spot, check off item, etc.) made while offline in Hive; drain the queue automatically on reconnect via `connectivity_plus`; surface conflict errors to the user (see `docs/features/phase 3/offline-strategy.md`).
- [ ] **238. Offline Phase 2 — structured data cache** — store last-fetched trips/spots/itinerary/stays/shopping in Hive using `hive_flutter`; load from Hive immediately on launch; refresh from Supabase in background; show subtle offline indicator when Supabase is unreachable (see `docs/features/phase 3/offline-strategy.md`).
- [ ] **237. Offline Phase 1 — image caching** — add `cached_network_image`; replace all `Image.network` and `NetworkImage` usages so recently-viewed images load from disk when offline (see `docs/features/phase 3/offline-strategy.md`).
- [x] **241. Fix: TripGate Riverpod state modified during build** — `_TripGateState.initState` was calling `TripNotifier.load()` synchronously; `load()` sets `state =` before its first `await`, violating Riverpod's "no provider mutation during build" rule; wrapped the call in `Future(() => ...)` to defer it past the current build frame; `features_test.dart` `setUpAll` now creates a test trip via the `create_trip_with_owner` RPC if the test user has no trips, so the nav bar and all tabs render correctly (2026-08-12, build 193).
- [x] **240. Realtime sync tests — all 11 feature tables** — expanded `realtime_sync_test.dart` to cover every table: packing INSERT+UPDATE, shopping INSERT+UPDATE, spots INSERT+UPDATE+DELETE, plan items INSERT+UPDATE, receipts INSERT+DELETE, documents INSERT+UPDATE, travel items INSERT+UPDATE, stays INSERT+UPDATE, links INSERT+DELETE, crew chat INSERT (both directions), trip members membership verification; all tests clean up after themselves (2026-08-12, build 192).
- [x] **239. Comprehensive integration tests: auth + all screens + CRUD** — `auth_test.dart` (22 tests): unauthenticated state, magic link, password mode (switch/invalid/valid credentials), sign-up validation, forgot password flow, sign out; `features_test.dart` (50+ tests): every bottom-nav tab, every More-row sub-screen, spot/plan/money/packing/shopping full CRUD, crew chat input and SOS dialog, trip switcher/settings/invite sheets, Riverpod correctness (all screens reload after trip switch); `helpers.dart` shared test utilities; run with `flutter test integration_test/ --dart-define-from-file=.env -d <device>` (2026-08-12, build 191).
- [x] **238. Integration tests — screen smoke + two-client realtime sync** — `integration_test/smoke_test.dart` signs in via Supabase, navigates all 5 bottom-nav tabs, opens More-screen sheets, and tests trip switching; `integration_test/realtime_sync_test.dart` creates two independent SupabaseClient instances (simulating mobile + browser) and verifies packing-item INSERT/UPDATE and crew-chat INSERT propagate via Supabase Realtime within 6 s; run with `flutter test integration_test/ --dart-define-from-file=.env -d <device>`; add `TEST_EMAIL`, `TEST_PASSWORD`, `TEST_EMAIL_2`, `TEST_PASSWORD_2` to `.env` before running (2026-08-12, build 190).
- [x] **237. Riverpod migration — replace TripState/ProfileState InheritedWidgets** — all screens that previously read from `TripState` or `ProfileState` InheritedWidgets now use Riverpod providers (`profileProvider`, `activeTripProvider`, `activeTripIdProvider`, `tripMembersProvider`, `tripNotifierProvider`); converted 14 files to `ConsumerStatefulWidget`/`ConsumerWidget`; deleted the `_pushWithState` helper from `more_screen.dart`; `app_shell.dart` trip switcher call updated; eliminates a whole class of null-access crashes when navigating before providers have loaded (2026-08-09, build 189, see `docs/features/phase 3/riverpod-migration.md`).
- [x] **236. Shopping list** — a shared shopping list screen is now accessible from the More tab (mobile) and the sidebar (desktop); add items with a quick bar at the bottom or tap the tune icon for name, quantity, notes, and an optional linked spot; check off items to move them to a "Checked" section; swipe left to delete individual items; clear all checked items at once via the AppBar action; realtime sync so the whole crew sees updates live; migration 031 adds the `shopping_items` table (2026-08-09, build 188).
- [x] **235. Spot ↔ stay linking** — accommodations can now be linked to spots; open a stay in the Add/Edit form and tap "Linked spot" to pick from the trip's spots or remove an existing link; a "Linked stay" card appears in the spot detail showing the stay name, status badge, and check-in/check-out date range; a "Linked spot" row appears in stay detail sheets (map and spots screen) showing the spot name and status; migration 030 adds `spot_id` FK to the `accommodations` table (2026-08-09, build 187).
- [x] **234. Stays in Spots screen** — accommodations now appear below the spots list in the Spots screen; a "Stays" section header with hotel icon separates them from spots; each stay shows as a card with a color-coded hotel icon, name, city, and status badge; tapping opens a mini sheet with name, status, city, address, check-in/check-out dates, and nights; realtime subscription extended to `accommodations` table; desktop layout also shows the stays section in the left panel (2026-08-09, build 186).
- [x] **233. Plan: calendar view** — tap the calendar icon in the Plan AppBar to switch from list to a month-grid calendar; days with itinerary items show colored dot indicators (one per item type); tapping a date shows the day's items with checkboxes and type icons; tap any item to open its detail screen; swipe left/right arrows to navigate months; toggle back to list view at any time (2026-08-09, build 185).
- [x] **232. Stays on map** — accommodations with coordinates now appear on the Map screen as purple hotel markers alongside spot markers; marker taps open a mini detail sheet showing name, status, address, check-in/check-out, and nights; list view shows separate "Stays on map" and "Stays without coordinates" sections; map camera fit and center calculations include both spots and stays; realtime subscription extended to `accommodations` table (migration 029 adds it to the Supabase realtime publication) (2026-08-08, build 184).
- [x] **231. Fix: crew chat + trip members not updating in real-time** — crew chat messages now appear instantly for the sender after every send (text, location ping, Find Me, meetup point) without waiting for the Supabase realtime subscription; realtime subscription for `trip_members` added to `TripGate` so new members appear without a restart; migration 028 adds `trip_members`, `location_shares`, and `message_reactions` to the Supabase realtime publication (using idempotent DO block; `trip_messages` was already in the publication) (2026-08-08, build 183).
- [x] **230. Add Spot UX polish (4 fixes)** — (1) Add button no longer hidden behind the phone's system navigation bar: scroll bottom padding now includes `viewPaddingOf.bottom` in addition to keyboard inset; (2) category auto-selects from search result, with name-based fallback inference (food/shopping/nature/experience/nightlife) when the place search returns the generic "landmark" default; (3) Address field now has a search icon that searches the typed address via PlaceSearchService and shows a results dropdown — selecting a result fills address, lat/lng, city, and country without overwriting existing fields; (4) "Search a place" field now keeps the selected place name visible after picking a result instead of clearing to empty (2026-08-08, build 182).
- [x] **229. Fix: Find Me SOS always showed "Could not get location"** — root cause was a missing `message_type` value in the `trip_messages` check constraint: the table only allowed `'text'` and `'location_ping'`; inserting `'find_me'` threw a PostgreSQL constraint violation which was caught and displayed as "Could not get location"; fixed by migration 027 which drops and recreates the constraint to include `'find_me'` and `'meetup_point'`; also added lastPosition caching in `LocationSharingManager` so the stream's most recent fix is used instantly if sharing is active (2026-08-08, build 181).
- [x] **228. Fix: push notifications — FCM JWT base64url encoding** — `send-notification` Edge Function was using standard base64 (`btoa`) for JWT components; `+` characters in the output were interpreted as spaces in the `application/x-www-form-urlencoded` OAuth body, causing Google to return `invalid_grant: Invalid signature`; added `toBase64Url()` to convert all JWT parts (header, payload, signature) to base64url, and wrapped the `assertion` value in `encodeURIComponent()`; also fixed `FCM_SERVICE_ACCOUNT` secret which was corrupt due to multi-line JSON breaking the CLI — re-set by minifying JSON first via PowerShell then `supabase secrets set --env-file`; function now returns `{"sent": 3, "total": 3}` (2026-08-08, build 180, see `supabase/functions/send-notification/index.ts`).
- [x] **227. Fix: location sharing crash when GPS disabled** — `Geolocator.getPositionStream().listen()` had no `onError` handler; when the emulator's location service was disabled mid-session a `LocationServiceDisabledException` propagated unhandled and `isSharing` stayed `true` permanently; added `onError:` + `cancelOnError: true` to the stream listener in `LocationSharingManager.start()` so any stream error calls `stop()` and resets state; crew screen now passes an `onError` callback that shows a user-facing message and opens Location Settings (2026-08-08, build 179).
- [x] **226. Money: clear all filters in one tap** — a "Clear filters" chip appears in the Receipts tab when any filter is active (category, payer, date range, or search); tapping it resets all filters and the search field at once (2026-08-08, build 178).
- [x] **225. Docs: share as formatted text** — a share button appears in the Documents screen (mobile AppBar and desktop top bar) when documents exist; shares the currently-visible document list grouped by type (title + amount/currency + notes) as plain text via the system share sheet; hidden on web (2026-08-07, build 177).
- [x] **224. Links: share as formatted text** — a share button appears in the Links AppBar when links exist; shares visible links grouped by category (title + URL + notes) as plain text via the system share sheet; hidden on web (2026-08-07, build 176).
- [x] **223. Stays: export as CSV** — a share button appears in the Stays AppBar when stays exist; exports currently-filtered stays (name, city, dates, nights, price, status, confirmation number, URL, notes) as CSV and opens the system share sheet; hidden on web (2026-08-07, build 175).
- [x] **222. Spots: sort by city** — a "By city" option added to the spots sort menu; sorts spots alphabetically by city, then spot name within each city — useful for planning day trips grouped by area (2026-08-07, build 174).
- [x] **221. Packing: pack all items at once** — a "Pack all items" option in the packing menu marks every remaining unpacked item as packed in a single tap; optimistic update with server sync; only shown when there are unpacked items remaining (2026-08-07, build 173).
- [x] **220. Spots: quick-filter chips by status** — a row of status chips appears below the category strip (mobile and desktop) showing only the statuses that actually exist in the trip; tapping a chip filters to that status; multi-select supported; hidden when all spots share the same status (2026-08-07, build 172).
- [x] **219. Packing: share list as text** — a "Share list" option in the packing menu exports the full packing list as formatted text (□ for unpacked, ✓ for packed) and opens the system share sheet; hidden on web; only visible when the list has at least one item (2026-08-07, build 171).
- [x] **218. Plan: collapse/expand all days at once** — an "unfold less/more" button appears in the Plan AppBar when a trip has 2+ days; tapping it collapses or expands all day cards at once; individual chevrons still work independently (2026-08-07, build 170).
- [x] **217. Plan: collapse/expand day items** — a chevron button appears next to each day's item count badge; tapping it collapses the items list so you can get a bird's-eye view of all days on long trips; tapping again expands (2026-08-07, build 169).
- [x] **216. Money: swipe receipt left to delete** — swipe a receipt card left in the mobile Receipts list to delete it immediately; optimistically removes it with a snackbar confirmation (2026-08-07, build 168).
- [x] **215. Money: payer filter on mobile** — the "filter by who paid" chip strip now appears in the mobile Receipts tab (below the category filter), matching the desktop; only shown when 2+ different payers exist (2026-08-07, build 167).
- [x] **214. Travel: swipe left to delete** — swipe a travel card left to delete it immediately; optimistically removes it with a snackbar; rolls back on failure (2026-08-07, build 166).
- [x] **213. Links: swipe left to delete** — swipe a link card left to delete it immediately; optimistically removes it with a snackbar confirmation; rolls back on failure; the existing long-press delete (with confirmation dialog) still works (2026-08-07, build 165).
- [x] **212. Stays: swipe left to delete** — swipe a stay card left to reveal a delete action; optimistically removes the card and shows a snackbar; rolls back with an error message if the deletion fails (2026-08-07, build 164).
- [x] **211. Spots: progress bar on desktop** — the "X of Y visited" progress bar that appeared in the mobile spots list now also shows in the desktop left panel, directly below the category filter strip (2026-08-07, build 163).
- [x] **210. Money: search cash withdrawals** — a search bar now appears on the Cash tab; matches by currency code, ATM notes, or formatted amount; shows a "no results" empty state when nothing matches; works alongside the existing sort and on both mobile and desktop (2026-08-07, build 162).
- [x] **209. Docs: filter by uploader** — a "who uploaded" filter chip row appears below the type strip when a trip has documents from 2+ people; tapping a member's chip shows only their uploads; counts shown on each chip; clears the selected doc (2026-08-07, build 161).
- [x] **208. Travel: export as CSV** — a share button appears in the Travel AppBar when items exist; tapping it exports all currently-visible travel items (respecting active type/status filters) as a CSV and opens the system share sheet (2026-08-07, build 160).
- [x] **207. Plan: FAB day picker** — "Add item" FAB now shows a day picker when a trip has multiple days instead of always appending to the last day; single-day trips still go straight to the add-item sheet (2026-08-07, build 159).
- [x] **206. Packing: swipe to pack/unpack** — swipe right on any packing list item to toggle it packed; a check icon slides in for unpacked items, an undo icon for packed; works alongside the existing checkbox tap and inside the reorderable list without conflicting with drag-to-reorder (2026-08-07, build 158).
- [x] **205. Global search: cash withdrawals** — cash withdrawals are now included in global search results; search matches by currency code, notes, or formatted amount; tapping a result opens the cash detail screen; "Cash" section appears grouped with other money results (2026-08-07, build 157).
- [x] **204. Stays: sort options** — sort button added to Stays AppBar with "By check-in date" (default), "A – Z", and "Newest added" options; icon turns blue when a non-default sort is active; stays with no check-in date fall to the end in date mode (2026-08-07, build 156).
- [x] **203. Home: spots visited fraction + up to 3 upcoming items** — "Spots visited" hero stat now shows "X/Y" fraction so you can see your progress at a glance; "Coming up" card now shows up to 3 upcoming items (plan days and travel bookings interleaved by date) instead of just one (2026-08-07, build 155).
- [x] **202. Money: cash summary card + sort** — when 2+ withdrawals exist a summary card appears at the top of the Cash tab showing total withdrawn per currency and ATM fees; sort button (newest/oldest) added to mobile AppBar and desktop panel; uses `_sortedWithdrawals` getter (2026-08-07, build 154).
- [x] **201. Plan: copy day as text** — a "Copy day as text" option appears in the day card menu (···); copies a formatted list of items with times to the clipboard, confirmed with a snackbar (2026-08-07, build 153).
- [x] **200. Links: sort options** — a sort button appears in the Links AppBar with "Newest first", "Oldest first", and "A – Z" options; highlighted in blue when a non-default sort is active (2026-08-07, build 152).
- [x] **199. Docs: sort options** — a sort button (↕) appears in the Documents AppBar and desktop top bar with "Newest first", "A – Z", and "By type" options; highlighted when a non-default sort is active (2026-08-07, build 151).
- [x] **198. Travel: booking status filter chips** — "Booked", "Tentative", and "Cancelled" status chips appear below the type filter on the Travel screen (only when more than one status is present); tap to filter; shows item count per status (2026-08-07, build 150).
- [x] **197. Plan: swipe to check/uncheck items** — swipe right on any plan item to toggle it done; a check icon slides in from the left; swipe right again to uncheck it; works alongside the existing checkbox dot tap (2026-08-07, build 149).
- [x] **196. Money: quick date preset chips** — "Today", "This week", and "This month" chips appear below the filter strip on the Receipts tab; tap a chip to filter receipts to that window; tap again to clear; works on both mobile and desktop (2026-08-07, build 148).
- [x] **195. Spots: visited progress bar** — a thin progress bar appears below the category filter strip showing "X of Y visited" with a percentage; turns green when all spots in the current view have been visited (2026-08-07, build 147).
- [x] **194. Home: today's travel bookings in agenda card** — when today is a plan day, any travel items (flights, hotels, etc.) for today appear in a "Bookings today" section at the bottom of the Today Agenda card (2026-08-07, build 146).
- [x] **193. Plan: day time-span in card header** — when 2 or more items on a day have a time set, the day card header shows a "09:00 → 21:00" span in monospace so you can see the shape of the day at a glance (2026-08-07, build 145).
- [x] **192. Plan: trip progress bar** — when at least one item is checked off, a compact progress bar appears below the search bar showing "X of Y items done" with a percentage; turns green when everything is complete (2026-08-07, build 144).
- [x] **191. Packing: comma-separated bulk add** — the "Add item" dialog now accepts comma-separated input so you can add "Passport, charger, adapter" in one go; a snack bar confirms how many items were added (2026-08-07, build 143).
- [x] **190. Travel: "jump to today" button** — a calendar icon appears in the Travel screen AppBar when today's date has a travel item; tapping it scrolls the list to today's date separator (2026-08-07, build 142).
- [x] **189. Plan: hide completed items toggle** — an eye icon appears in the plan AppBar when any items are checked off; tapping it hides all completed items from every day card so you can focus on what's left; tap again to show them (2026-08-07, build 141).
- [x] **188. Money: daily average spend in spending summary** — the spending-by-category card now shows "~¥X,XXX/day" next to the grand total, computed from the number of distinct days that have receipts (2026-08-07, build 140).
- [x] **187. Plan: per-day cost total in day card header** — each day card now shows a teal pill with the total planned cost for that day when any items have a planned cost set (2026-08-07, build 139).
- [x] **186. Plan: "Jump to today" button** — a calendar icon appears in the plan screen AppBar on days when today has a plan day; tapping it scrolls the list to today's day card (2026-08-07, build 138).
- [x] **185. Travel: date separators in travel list** — travel items are now grouped by date with a "Jan 12" header between each date group; "Today" and "Tomorrow" labels are shown for today/tomorrow's items; undated items fall under a "No date" section at the end (2026-08-07, build 137).
- [x] **184. Plan: budget summary bar + planned cost on item detail** — plan screen now shows a "Planned · ¥85,500 + $1,200" banner below the search bar (hidden when no costs are set); plan item detail now shows "Est. cost" in the meta card when a planned cost is set; totals are summed per currency across all plan days (2026-08-07, build 136).
- [x] **183. Travel: link back after "Add to itinerary"** — after adding a travel item to the itinerary, the travel item record in DB is now updated with `linked_day_id` and `linked_itinerary_item_id`; the "In plan" badge appears immediately in the travel list; added `TravelService.linkToDay` method (2026-08-07, build 135).
- [x] **182. Travel cards: "In plan" and doc badges** — travel item cards now show an "In plan" teal badge when the booking has been added to the itinerary, and a purple doc count badge when documents are linked; makes it easy to see which bookings are already planned and which have supporting docs (2026-08-07, build 134).
- [x] **181. Plan item: "Attach Document" wired up** — tapping "Attach Document" on a plan item now opens the doc-picker sheet; previously linked documents are pre-checked; syncs via the new `PlanService.syncDocLinks`; shared `showDocAttachSheet` now reused across both plan and travel detail screens (2026-08-07, build 133).
- [x] **180. Travel: "Attach document" wired up** — tapping "Attach document" on a travel item opens a doc-picker sheet showing all trip documents with checkboxes; previously linked docs are pre-checked; tapping "Save" syncs the links to DB; also wired in the overflow (⋮) actions sheet (2026-08-07, build 132).
- [x] **179. Travel: "Add to itinerary" wired up** — tapping "Add to itinerary" on a travel item now shows the day picker sheet; choosing a day (and optional time) creates a real plan item with the correct type mapping (flight → Travel, train → Transport, ticket → Activity); shows a success or error snackbar; button label changes to "Adding…" while saving to prevent double-taps (2026-08-07, build 131).
- [x] **178. Travel items: booking URL field** — travel items now support an optional booking URL; add it in the edit form; tapping "Open booking →" in the actions section opens the airline/rail/hotel manage-booking page in the browser; stored in DB via migration 025 (2026-08-07, build 130).
- [x] **177. Spots: long-press quick status + fix comment author names** — long-pressing a spot tile shows a quick-action sheet to mark it visited, skip it, or reset it without opening the detail screen; plan item comments now show real member names instead of UUID substrings (2026-08-07, build 129).
- [x] **176. Plan item: tappable Maps & confirmation links + linked spot navigation** — the "Google Maps" and "Confirmation" rows in the plan item detail card are now tappable (show "Open →" link in primary colour); tapping opens the URL in the browser; the linked spot card navigates to the full Spot Detail screen instead of showing a placeholder snackbar (2026-08-07, build 128).
- [x] **175. Home: "Spots visited" hero stat** — trip hero now shows "Spots visited" (count of spots marked as Visited) instead of total spots saved; gives a quick sense of progress through the trip rather than raw list size (2026-08-07, build 127).
- [x] **174. Packing list: "Mine only" filter** — a "Mine only" filter chip appears on the packing list when at least one item has been assigned; activating it shows only items assigned to you, making it easy to find and pack your own items in a large group; reorder handles are hidden while the filter is active (2026-08-07, build 126).
- [x] **173. Map: long-press to add spot** — long-pressing anywhere on the map opens the Add Spot sheet with the tapped coordinates pre-filled; the "Map ready" indicator appears immediately so users know the pin is set; adding the name and saving drops the new spot marker on the map in real time (2026-08-07, build 125).
- [x] **172. Travel item past-date dimming** — travel items with a departure date in the past are now shown at 65% opacity so completed bookings recede visually; the countdown label changes to "Yesterday" or "N days ago" in muted grey (vs. the coloured upcoming labels); cancelled items are unchanged (2026-08-07, build 124).
- [x] **171. Receipts: "Paid by" person filter** — a second filter strip appears in the Money → Receipts tab when 2+ different people have paid receipts; shows each payer's name and receipt count as chips; composable with the existing category, date range, search, and sort filters; hidden for solo trips (2026-08-07, build 123).
- [x] **170. Stays: live DB + confirmation number** — `accommodations` table now exists in the remote Supabase DB (migration 024, was missing — the service was silently falling back to mock data); `confirmation_number` column added to the table and exposed through the full stack (model, service, add/edit sheet); accommodation cards show the confirmation number with a one-tap copy button; a tappable "Open booking →" link appears on cards that have a URL (2026-08-07, build 122, see `docs/features/phase 3/accommodation-improvements.md`).
- [x] **169. Crew chat: emoji reactions** — long-press any text message in crew chat to get a reaction picker (❤️ 👍 😂 😮 😢 🔥); reactions appear below the bubble as pills with a count; tap a reaction pill to add/remove your own; reactions sync in real-time via the new `message_reactions` Supabase table (migration 023); highlighted when you've reacted (2026-08-07, build 121, see `docs/features/phase 3/crew-reactions.md`).
- [x] **168. Packing list: progress bar + template quick-add** — visual `LinearProgressIndicator` (rounded, green when all packed) between the search bar and the item list shows "X of Y packed" / "All packed!"; overflow menu gains "Add from template" entry and the empty state shows an outlined "Add from template" button; tapping opens a `DraggableScrollableSheet` with 6 travel categories (Documents, Toiletries, Clothes, Electronics, Health & Meds, Comfort & Entertainment) — expand to see checkboxes, close to deselect, "Add N items" button adds only items not already on the list (2026-08-07, build 120, see `docs/features/phase 3/packing-improvements.md`).
- [x] **166. "Open email app" button on magic-link sent screen** — after entering an email and tapping "Send magic link" (or any other "check your inbox" state: password reset, sign-up confirmation), a primary "Open email app" button now appears above the back link on mobile (hidden on web); taps `mailto:` so Android opens the user's default email client directly (2026-08-07, build 119).
- [x] **165. SOS button redesign + high-priority alert** — "Find Me" SOS button moved out of the tiny icon row into a full-width, prominent red `FilledButton` above the chat message field; a confirmation dialog ("Alert your crew?") prevents accidental taps; on confirm, crew members receive a high-priority push notification that bypasses silent mode on Android (already `android.priority: high`) and on iOS via `apns-priority: 10` + `content-available`; `pushNotify()` gains a `highPriority` flag that passes `high_priority: true` to the `send-notification` Edge Function (2026-08-07, build 118, see `docs/features/phase 3/sos-button.md`).
- [x] **164. Update banner shown before login** — extracted `_UpdateBanner` from home_screen into shared `UpdateCheckerBanner` widget; `UpdateChecker.check()` now caches its result so only one GitHub API request is made per session; banner appears on the sign-in/sign-up page and the no-trips screen in addition to the home screen (2026-08-07, build 117).
- [x] **163. Invite links + Android download banner** — shareable invite URLs (`https://wabway.wabble.ca/?invite=CODE`) generated from Invite Codes sheet via new "Share link" button; `InviteLinkHandler` singleton reads invite code from URL query params (web) or Android App Links MethodChannel (native); pending code survives sign-up via SharedPreferences and auto-opens the Join sheet after login; Android browser (non-iPhone, non-desktop) web visitors see a compact bottom banner recommending the Android app on both the home screen and sign-in/sign-up page; banner is dismissible and persists dismissed state (2026-08-07, build 116, see `docs/features/phase 3/invite-links.md`).
- [x] **162. Fix invite code "something went wrong" + add email invite** — root cause: `revoked_at` column was added to `trip_invites` manually after the `redeem_trip_invite` Postgres function was compiled; the `%ROWTYPE` variable in the function didn't include the new column causing a runtime error on every redeem attempt; fix: migration `022_trip_invites_revoked_at.sql` uses `CREATE OR REPLACE` to recompile the function (also adds `revoked_at IS NULL` check so revoked codes are properly rejected); also fixed `_JoinTripProxy` in `trip_switcher_sheet.dart` which was discarding the join result (`showModalBottomSheet<void>`) and never calling `onJoined`; added email invite flow to `add_member_sheet.dart` — when an email has no WabWay account, offers "Send email invite" which generates an invite code and opens the device mail app with a pre-composed message (2026-08-07, build 115).
- [x] **161. Location sharing persists when navigating away from Crew** — extracted `LocationSharingManager` singleton (`lib/core/location/location_sharing_manager.dart`) that owns the GPS stream; `CrewScreen.dispose` no longer cancels the stream or deactivates the share — sharing keeps running until the user explicitly stops it (via the toggle, the notification Stop button, or app termination) (2026-08-07, build 114).
- [x] **160. Location sharing notification — non-dismissible + Stop action** — added `setOngoing: true` to `ForegroundNotificationConfig` so the notification can't be swiped away while sharing; added native Android `StopLocationReceiver` BroadcastReceiver and `addStopActionToNotification()` in `MainActivity`; on the first GPS position the Dart side calls `addStopAction` via MethodChannel which rebuilds the geolocator notification (ID 75415) with a "Stop sharing" action button; tapping it fires the broadcast → Flutter stops the stream and deactivates the share (2026-08-07, build 113).
- [x] **159. Money page: summary cards scroll with the list** — Receipts tab was a `Column` with the "Spending by category" and "Spending by member" cards fixed at the top, leaving only a small `Expanded` area for the receipt list; replaced with a `CustomScrollView` + `SliverList` so the entire page scrolls as one unit (2026-08-07, build 112).
- [x] **158. Fix missing image when importing a spot** — spots saved via the Import screen (Google Maps link, social media caption extraction) were created without a photo; `WikipediaImageService.fetchThumbnailUrl` is now called before `SpotService.createSpot` in `incoming_share_screen.dart` and `extracted_spots_screen.dart`, matching what the Maps import CSV/list flow already did (2026-08-07, build 111).

---

## Dead buttons / stubs to wire up

| Location | Button | What it needs |
|---|---|---|
| `home_screen.dart:164,194` | Bell / notifications icon | Route to notification settings screen (see #18) |
| `home_screen.dart:370` | "Settle up" | Real settlement flow (see #17) |
| `more_screen.dart:222` | Notifications settings row | Notification settings screen (see #18) |
| `spots_screen.dart:386` | Filter icon | ~~Advanced filter sheet~~ — **done**, filter sheet for status + city already implemented |
| `incoming_share_screen.dart:94` | Travel / Links / Map share destinations | Wire to real flows (see #5) |
