Base surface container for every domain card in this system, plus `PhotoSlot`, the warm tinted placeholder used wherever a real photo hasn't been added yet.

```jsx
<Card hoverable onClick={() => {}}>…</Card>
<PhotoSlot icon={<Icon name="image" />} label="Add a cover photo" />
```
