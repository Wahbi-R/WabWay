Underline-style tab switcher — used in a Spot/Document/Receipt detail panel to switch between e.g. Details / Comments / Activity.

```jsx
<Tabs tabs={[{value:'details',label:'Details'},{value:'comments',label:'Comments'}]} value={tab} onChange={setTab} />
```
