Failed-load state — a trip that won't sync, a document that won't open.

```jsx
<ErrorState icon={<Icon src="../../assets/icons/wifi-off.svg" />} title="Couldn't load this trip" action={<Button variant="ghost">Try again</Button>} />
```
